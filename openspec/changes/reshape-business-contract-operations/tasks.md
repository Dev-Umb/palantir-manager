## 1. 聚焦工作区与元数据

- [x] 1.1 增加明确的保留对象访问策略，客户、联系人、项目和合同提供业务对象导航及按角色授权的直接增删改查入口。
- [x] 1.2 保留隐藏记录和客户/联系人关系查询，同时关闭隐藏对象对项目流程和财务的自动同步。
- [x] 1.3 更新项目元数据：生命周期状态、合同汇总状态、负责业务员、首次/末次发货日期、财务字段，并移除责任岗位和合同编号。
- [x] 1.4 保留客户/联系人独立列表和维护能力，同时支持在有权限的项目编辑界面搜索、选择、新增和修改，并保留原记录和快照。

## 2. 权限与业务员分配

- [x] 2.1 校验并保存负责业务员账号；迁移可明确匹配的现有创建人，不虚构分配关系。
- [x] 2.2 按负责账号限制业务员的项目和关联合同可见性。
- [x] 2.3 在服务端执行管理员、业务员、财务的字段读写矩阵，并把催款次数设为系统只读。
- [x] 2.4 测试隐藏对象直达拒绝、历史保留、客户/联系人可见与维护路径、业务员相互隔离、财务范围和管理员范围。

## 3. 合同附件与状态同步

- [x] 3.1 为加工函、合同、对账单增加可累加的多附件控件和服务端存储标准化。
- [x] 3.2 执行加工函/合同附件硬校验，并保证追加和删除操作安全。
- [x] 3.3 覆盖零合同、单合同、多合同、部分签署、全部签署的项目汇总，且项目不显示合同编号。
- [x] 3.4 执行人工项目状态附件校验、保护 `已完成`，并在新增未签合同后恢复提醒。
- [x] 3.5 增加 PHPUnit 和 Vitest：正常、失败、历史文件、多文件、多合同、回滚和客户关系相邻路径。

## 4. 项目财务维护

- [x] 4.1 十个财务字段（含末次回款日期）允许财务编辑、业务员只读，催款次数仅由系统维护。
- [x] 4.2 项目合同金额为空时首次自动带入；财务手工修改后阻止后续合同变化自动覆盖。
- [x] 4.3 增加财务/管理员“从合同表重新同步”操作，记录来源、时间、操作人、权限校验和审计。
- [x] 4.4 停止财务台账/开票记录覆盖项目，并测试历史数据仍保留、手工值不回退。
- [x] 4.5 在项目表新增末次回款日期，纳入财务权限、日期筛选排序及回款提醒重置，并迁回已录入项目的历史日期。

## 5. 生命周期提醒

- [x] 5.1 增加可回滚的持久化提醒周期，并保守迁移起算时间。
- [x] 5.2 实现投标 15 天、中标后加工函 15 天、加工函后 3 个月再每 15 天催签、每月催款规则。
- [x] 5.3 按已确认人员分发提醒；每周期重置未读；条件消失时解决；`已完成` 停止全部提醒。
- [x] 5.4 财务更新回款数据后重置周期，每个项目到期周期只增加一次催款次数。
- [x] 5.5 使用冻结时间测试首次到期、重复、月末边界、重置、完成、重新激活、接收人、未读/历史、幂等和审计。

## 6. 多条件筛选与界面

- [x] 6.1 增加类型化项目/合同筛选解析、操作符、日期区间、多选归属和权限安全的 AND/OR 查询。
- [x] 6.2 增加筛选构建器：条件增删、满足全部/任一、错误提示、清空和 URL 持久化。
- [x] 6.3 更新项目/合同表格、表单、详情、仪表盘、通知和角色控件。
- [x] 6.4 测试筛选语义、错误参数、越权边界、URL 恢复、字段可见性，并保留分页与搜索行为。

## 7. 迁移、数据与验收

- [x] 7.1 演练 migrate → rollback → migrate，证明隐藏记录、客户关系、附件和旧 payload 均保留。
- [x] 7.2 开发中运行聚焦 PHPUnit/Vitest，并用 Pint 格式化修改过的 PHP。
- [x] 7.3 运行完整应用测试、前端测试、生产构建、严格 OpenSpec 校验和 `composer quality:gate`。
- [x] 7.4 测试通过后，向本地数据库写入具有真实感但虚构的公司、联系人、项目、多合同、附件、财务、归属、筛选和提醒示例。
- [x] 7.5 启动本地应用和提醒所需调度/队列，解析绝对本地 URL，并用浏览器覆盖管理员、业务员、财务端到端流程。
- [x] 7.6 分别报告代码、迁移、测试、种子数据、服务和本地浏览器证据；不声称 PR、合并、部署或线上验证。
