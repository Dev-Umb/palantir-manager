## 1. 评审与口径冻结

- [x] 1.1 评审并批准 scope contract、四项首要 KPI、四个图表、空值语义与“累计实际发生金额”产值口径。
- [x] 1.2 确认实施继续使用既有角色、权限、对象与表结构，不新增任何持久化事实或权限记录。
- [x] 1.3 确认 `tender` 能力进入实施基线；若未进入，第一版条件性省略招投标面板。
- [x] 1.4 `composer openspec:validate` 严格校验本变更。

## 2. 只读服务端聚合（批准后实施）

- [x] 2.1 在现有 Dashboard 读取链路内增加固定数量的权限感知聚合，不新增 route、migration、Model 写入、Job、Command 或持久化缓存。
- [x] 2.2 实现累计实际发生金额、加权回款率、当前欠款与投标中标率，并返回分子、分母、覆盖和空值状态。
- [x] 2.3 实现合同到现金、当前招投标管线、项目 `overall_status` 构成、生产任务与按 `ship_date` 月汇总的发货吨位趋势；来源对象不存在或无权限时从服务端响应省略。
- [x] 2.4 L2 PHPUnit：公式、零分母、未知项目状态、负/非数值吨位、缺失发货日期、管理员全量、业务员项目范围与仅 `dashboard.view` 的安全空态。
- [x] 2.5 L2 无副作用回归：请求前后业务对象、记录、审计日志与业务时间戳不变。

## 3. 驾驶舱界面（批准后实施）

- [x] 3.1 在现有 Dashboard 顶部增加范围/截至时间、四项 KPI、风险提醒和四个可视化面板，沿用当前视觉语言，并使用 Recharts `PieChart`/环形图与 `LineChart` 展示项目构成和月度发货吨位。
- [x] 3.2 所有图表提供数值、单位、图例、覆盖和文本摘要；折线图覆盖负/非数值吨位及缺日期边界，环形图覆盖未知/空项目状态并提供完整文字等价内容；无数据、无权限和零分母安全呈现。
- [x] 3.3 保留既有项目状态、风险提醒、最近项目和合同下钻入口，完成旧能力到新布局的逐项映射。
- [x] 3.4 L3 Vitest：图表、文本摘要、空态、缺失指标及既有能力保留。

## 4. 验证与交付（批准后实施）

- [x] 4.1 `composer test:narrow -- <dashboard-feature-tests>` 通过。
- [x] 4.2 `npm run test:ui -- resources/js/Pages/Dashboard.test.jsx` 通过。
- [x] 4.3 `npm run build` 通过。
- [x] 4.4 PHP 有改动时运行 `vendor/bin/pint --dirty --format agent`。
- [x] 4.5 `composer openspec:validate` 与 `composer quality:gate` 通过，复核最终 diff 不含任何 schema、业务对象、角色、权限或数据写入改动。

> 用户已明确批准实施；本 change 未归档。

## 5. 项目推进切换

- [x] 5.1 删除“项目当前走到哪一步”标题，保留项目推进语义。
- [x] 5.2 服务端只读返回全部可见项目的阶段摘要，默认最近项目且不增加查询或写入。
- [x] 5.3 前端项目选择器切换阶段状态和项目详情入口，覆盖桌面端与移动端。
- [x] 5.4 PHPUnit、Vitest、构建、UI 走查与端到端切换验证通过。
