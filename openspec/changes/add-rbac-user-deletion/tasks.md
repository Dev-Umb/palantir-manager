## 1. 持久化与模型

- [x] 1.1 使用 Artisan 新增 `users.deleted_at` 可逆迁移。
- [x] 1.2 `User` 启用 `SoftDeletes`，保持现有关系、填充和隐藏字段不变。

## 2. 删除与保护规则

- [x] 2.1 增加受 `rbac.manage` 保护的单用户 `DELETE` 路由。
- [x] 2.2 在事务中实现自删保护、最后管理员保护、角色解除、数据库会话清理、软删除与审计。
- [x] 2.3 角色更新接口同步阻止移除最后一个有效管理员角色。
- [x] 2.4 RBAC 页面 props 返回每个用户的 `can_delete` 和明确禁用原因。

## 3. 前端交互

- [x] 3.1 用户卡片增加删除按钮、二次确认和处理中状态。
- [x] 3.2 自己与最后管理员的删除按钮禁用并展示原因；角色保存行为保持不变。

## 4. 回归验证

- [x] 4.1 L2 Feature：普通用户删除成功、角色与 session 清理、历史关联保留、审计记录存在。
- [x] 4.2 L2 Feature：自删、最后管理员删除、最后管理员角色移除和无权限访问均拒绝；多个管理员时允许删除其中一个。
- [x] 4.3 L3 Vitest：删除确认、取消、处理中状态、禁用原因与既有角色保存回归。
- [x] 4.4 运行 Pint、聚焦测试、OpenSpec 严格校验与完整 `composer quality:gate`。
